# math-question

This is just a quick program I made to get a better understanding of _variables_ and _functions_. It's just a function called **math_conversation()**. The first two parameters take 2 integers while the last 2 take names as strings. Then it prints out a conversation between a student and a teacher with different outputs based on what arguments are inputted into the function.

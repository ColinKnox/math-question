# math-question

This is just a quick program to get a better understanding of variables and functions. All it is is a function called math_question(). The first 2 parameters take to integers while the last 2 take names as strings. Then it prints out a conversation between a student and a teacher while different outputs based on what arguments are inputted into the function.
